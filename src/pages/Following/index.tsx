import React from 'react';
import LoginForm from '../../components/LoginForm/LoginForm';

const Following = () => {
    return (
        <div>
            <LoginForm></LoginForm>
        </div>
    );
};

export default Following;
